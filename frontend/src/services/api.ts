const API=import.meta.env.VITE_API_URL || 'http://localhost:8000';
export async function request(path:string, options:RequestInit={}) { const token=localStorage.getItem('kshamta_token'); const response=await fetch(`${API}${path}`,{...options,headers:{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{}) ,...(options.headers||{})}}); if(!response.ok) throw new Error((await response.json().catch(()=>({detail:'Request failed'}))).detail||'Request failed'); return response.json(); }
export const api={login:(username:string,password:string)=>request('/api/auth/login',{method:'POST',body:JSON.stringify({username,password})}),dashboard:()=>request('/api/dashboard/overview'),analytics:(q='')=>request(`/api/projects/analytics${q}`),risk:()=>request('/api/projects/risk'),workforce:()=>request('/api/workforce/overview'),employees:()=>request('/api/employees'),employee:(id:string)=>request(`/api/employees/${id}`),project:(id:string)=>request(`/api/projects/${id}`),recommend:(x:any)=>request('/api/resources/recommend',{method:'POST',body:JSON.stringify(x)}),whatIf:(id:string)=>request('/api/resources/what-if',{method:'POST',body:JSON.stringify({employee_id:id})}),metrics:()=>request('/api/attrition/model-metrics'),predict:(x:any)=>request('/api/attrition/predict',{method:'POST',body:JSON.stringify(x)})};
