export function KPI({label,value}:{label:string,value:any}){return <div className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><p className="text-xs font-medium uppercase tracking-wide text-slate-500">{label}</p><p className="mt-2 text-2xl font-semibold text-slate-900">{value}</p></div>}
export function Badge({value}:{value:string}){const color=value==='High'||value==='Avoid'?'bg-red-100 text-red-700':value==='Medium'||value==='Consider'?'bg-amber-100 text-amber-700':'bg-emerald-100 text-emerald-700';return <span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${color}`}>{value}</span>}
export function Table({rows}:{rows:any[]}){if(!rows?.length)return <div className="rounded-xl bg-white p-8 text-center text-slate-500">No data is available for this selection.</div>;const cols=Object.keys(rows[0]).filter(k=>!['task_records','tasks','prediction'].includes(k));return <div className="overflow-auto rounded-xl border border-slate-200 bg-white"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr>{cols.map(c=><th className="whitespace-nowrap px-4 py-3" key={c}>{c.replaceAll('_',' ')}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr className="border-t border-slate-100" key={i}>{cols.map(c=><td className="whitespace-nowrap px-4 py-3" key={c}>{typeof r[c]==='number'?Number(r[c]).toFixed(1).replace('.0',''):String(r[c])}</td>)}</tr>)}</tbody></table></div>}
